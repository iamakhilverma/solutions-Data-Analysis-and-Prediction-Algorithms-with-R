# Assigning the values to coefficients --------------------------------
a <- 2
b <- -1
c <- -4
# Solutions to the equation ax^2 + bx + c = 0 -------------------------
solution_1 <- (-b + sqrt(b^2 - 4 * a * c)) / (2 * a)
solution_2 <- (-b - sqrt(b^2 - 4 * a * c)) / (2 * a)
